import React from 'react';

import './Chat.css';

// const ChatList = (props) => (

// );
// const ChatBox = (props) => (

// )
const Chat = (props) => {
    return (
        <div className="chat-box">
            <div className="chat-list"></div>
            <div className="chat">

            </div>
        </div>
    )
}

export default Chat;